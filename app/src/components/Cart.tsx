import React, { FC } from 'react'
import { Image, Button } from 'react-bootstrap'

const Cart: FC = () => {
    return (
      <div className="outermost-div">

      CART
      </div>
    );
  }
  
  export default Cart;